# CI scenario: minimal vanilla server
server:
  eula: true
  type: VANILLA
  version: LATEST
  maxPlayers: 10

jvm:
  memory: 512M

persistence:
  enabled: true
  size: 1Gi

resources:
  requests:
    cpu: 250m
    memory: 512Mi
  limits:
    cpu: "1"
    memory: 1Gi
