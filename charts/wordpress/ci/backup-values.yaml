# CI: Backup enabled with S3
backup:
  enabled: true
  schedule: "0 4 * * *"
  s3:
    endpoint: https://s3.example.com
    bucket: wordpress-backups
    prefix: wordpress
    accessKey: test-key
    secretKey: test-secret
