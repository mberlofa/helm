# CI: external PostgreSQL database
postgresql:
  enabled: false

database:
  type: postgresql
  external:
    host: db.example.com
    port: "5432"
    name: guacamole_db
    username: guacamole_user
    password: external-password
