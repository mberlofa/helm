# CI: monitoring enabled with ServiceMonitor
tunnel:
  token: "ci-test-token"

serviceMonitor:
  enabled: true
  interval: 15s
  labels:
    release: prometheus

service:
  annotations:
    prometheus.io/scrape: "true"
