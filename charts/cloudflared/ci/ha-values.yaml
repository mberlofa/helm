# CI: high-availability with 3 replicas and resource limits
tunnel:
  token: "ci-test-token"

replicaCount: 3

pdb:
  enabled: true
  minAvailable: 2

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    memory: 128Mi
