suite: PodDisruptionBudget
templates:
  - pdb.yaml
release:
  name: test
  namespace: default
tests:
  - it: should not create PDB by default
    asserts:
      - hasDocuments:
          count: 0

  - it: should not create PDB in single-node even when enabled
    set:
      pdb.enabled: true
    asserts:
      - hasDocuments:
          count: 0

  - it: should create PDB in cluster mode when enabled
    set:
      architecture: cluster
      pdb.enabled: true
    asserts:
      - isKind:
          of: PodDisruptionBudget
