# CI: external MariaDB database
database:
  type: mariadb
  external:
    host: db.example.com
    port: "3306"
    name: uptime_kuma
    username: kuma_user
    password: "ci-password"

mysql:
  enabled: false
