workload:
  enabled: false

imageTagFormat: simple
image:
  repository: myapp
  tag: "1.0.0"

jobs:
  - name: db-migrate
    command: ["npm", "run", "migrate"]
    backoffLimit: 3

cronjobs:
  - name: cleanup
    schedule: "0 2 * * *"
    command: ["npm", "run", "cleanup"]
