# CI: Metrics and ServiceMonitor enabled
config:
  telemetry:
    metrics:
      enabled: true
      address: "tcp://:9959/metrics"

metrics:
  enabled: true
  serviceMonitor:
    enabled: true
    interval: 30s
    scrapeTimeout: 10s
