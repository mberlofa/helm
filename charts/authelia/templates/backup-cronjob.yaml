{{- if .Values.backup.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "authelia.fullname" . }}-backup
  labels:
    {{- include "authelia.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.backup.schedule | quote }}
  suspend: {{ .Values.backup.suspend }}
  concurrencyPolicy: {{ .Values.backup.concurrencyPolicy }}
  successfulJobsHistoryLimit: {{ .Values.backup.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ .Values.backup.failedJobsHistoryLimit }}
  jobTemplate:
    spec:
      backoffLimit: {{ .Values.backup.backoffLimit }}
      template:
        metadata:
          labels:
            {{- include "authelia.selectorLabels" . | nindent 12 }}
            app.kubernetes.io/component: backup
        spec:
          restartPolicy: OnFailure
          initContainers:
            {{- $dbType := include "authelia.dbType" . }}
            {{- if eq $dbType "sqlite" }}
            - name: sqlite-backup
              image: "{{ .Values.backup.images.sqlite.repository }}:{{ .Values.backup.images.sqlite.tag }}"
              imagePullPolicy: {{ .Values.backup.images.sqlite.pullPolicy }}
              command: ["/bin/sh", "/scripts/sqlite-backup.sh"]
              {{- with .Values.backup.resources }}
              resources:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              volumeMounts:
                - name: data
                  mountPath: /data
                  readOnly: true
                - name: backup-scratch
                  mountPath: /backup
                - name: scripts
                  mountPath: /scripts
            {{- else if eq $dbType "postgres" }}
            - name: pg-backup
              image: "{{ .Values.backup.images.postgresql.repository }}:{{ .Values.backup.images.postgresql.tag }}"
              imagePullPolicy: {{ .Values.backup.images.postgresql.pullPolicy }}
              command: ["/bin/sh", "/scripts/pg-backup.sh"]
              env:
                - name: DB_HOST
                  value: {{ include "authelia.dbHost" . | quote }}
                - name: DB_PORT
                  value: {{ include "authelia.dbPort" . | quote }}
                - name: DB_NAME
                  value: {{ include "authelia.dbName" . | quote }}
                - name: DB_USER
                  value: {{ include "authelia.dbUsername" . | quote }}
                - name: DB_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "authelia.dbSecretName" . }}
                      key: {{ include "authelia.dbSecretPasswordKey" . }}
              {{- with .Values.backup.resources }}
              resources:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              volumeMounts:
                - name: backup-scratch
                  mountPath: /backup
                - name: scripts
                  mountPath: /scripts
            {{- else }}
            - name: mysql-backup
              image: "{{ .Values.backup.images.mysql.repository }}:{{ .Values.backup.images.mysql.tag }}"
              imagePullPolicy: {{ .Values.backup.images.mysql.pullPolicy }}
              command: ["/bin/sh", "/scripts/mysql-backup.sh"]
              env:
                - name: DB_HOST
                  value: {{ include "authelia.dbHost" . | quote }}
                - name: DB_PORT
                  value: {{ include "authelia.dbPort" . | quote }}
                - name: DB_NAME
                  value: {{ include "authelia.dbName" . | quote }}
                - name: DB_USER
                  value: {{ include "authelia.dbUsername" . | quote }}
                - name: DB_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "authelia.dbSecretName" . }}
                      key: {{ include "authelia.dbSecretPasswordKey" . }}
              {{- with .Values.backup.resources }}
              resources:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              volumeMounts:
                - name: backup-scratch
                  mountPath: /backup
                - name: scripts
                  mountPath: /scripts
            {{- end }}
          containers:
            - name: upload
              image: "{{ .Values.backup.images.uploader.repository }}:{{ .Values.backup.images.uploader.tag }}"
              imagePullPolicy: {{ .Values.backup.images.uploader.pullPolicy }}
              command: ["/bin/sh", "/scripts/upload-backup.sh"]
              env:
                - name: S3_ENDPOINT
                  value: {{ .Values.backup.s3.endpoint | quote }}
                - name: S3_BUCKET
                  value: {{ .Values.backup.s3.bucket | quote }}
                - name: S3_PREFIX
                  value: {{ .Values.backup.s3.prefix | quote }}
                - name: S3_ACCESS_KEY
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "authelia.backupSecretName" . }}
                      key: {{ .Values.backup.s3.existingSecretAccessKeyKey | default "access-key" }}
                - name: S3_SECRET_KEY
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "authelia.backupSecretName" . }}
                      key: {{ .Values.backup.s3.existingSecretSecretKeyKey | default "secret-key" }}
              {{- with .Values.backup.resources }}
              resources:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              volumeMounts:
                - name: backup-scratch
                  mountPath: /backup
                  readOnly: true
                - name: scripts
                  mountPath: /scripts
          volumes:
            {{- if eq $dbType "sqlite" }}
            - name: data
              persistentVolumeClaim:
                claimName: {{ include "authelia.dataClaimName" . }}
            {{- end }}
            - name: backup-scratch
              emptyDir: {}
            - name: scripts
              configMap:
                name: {{ include "authelia.fullname" . }}-backup-scripts
                defaultMode: 0755
{{- end }}
