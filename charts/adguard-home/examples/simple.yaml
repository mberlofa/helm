# Simple deployment — wizard mode with persistent storage and DNS LoadBalancer
# After install, access port 3000 to complete the setup wizard.

service:
  dns:
    type: LoadBalancer
    loadBalancerIP: "192.168.1.53"
