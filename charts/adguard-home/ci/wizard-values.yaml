# CI scenario: wizard mode (no config, uses port 3000 for initial setup)
# This is the default behavior — no config.adGuardHome is set.

service:
  dns:
    type: ClusterIP

persistence:
  conf:
    size: 128Mi
  work:
    size: 1Gi
