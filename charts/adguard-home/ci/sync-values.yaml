# CI scenario: AdGuard Home with adguardhome-sync enabled
config:
  adGuardHome:
    http:
      address: 0.0.0.0:80
    users:
      - name: admin
        password: "$2y$10$hW1sBKsFxvB8gKIYQl1w4O/4LHDoqq1mA92Dg6rNULYAx4YPamq0K"
    dns:
      bind_hosts:
        - 0.0.0.0
      port: 53
      upstream_dns:
        - https://dns.cloudflare.com/dns-query
      bootstrap_dns:
        - 1.1.1.1
    schema_version: 29

service:
  dns:
    type: ClusterIP

sync:
  enabled: true
  origin:
    url: "http://adguard-primary:80"
    username: admin
    password: changeme
  replicas:
    - url: "http://adguard-replica-1:80"
      username: admin
      password: changeme
    - url: "http://adguard-replica-2:80"
      username: admin
      password: changeme
